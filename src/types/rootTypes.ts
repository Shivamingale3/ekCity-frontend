import type { ReactNode } from "react";

export type HeroItem = {
  icon?: ReactNode;
  title: string;
  subtitle: string;
};

export type FeatureItem = {
  icon?: ReactNode;
  title: string;
  subtitle: string;
};
